6.ค้นหาจาก internet เพราะปัญหาของเราอาจจะมีคำตอบอยู่แล้วหรือมีใครเคยเจอมาก่อน
7.เป็นแบ่งการทำงานหลักๆให้เป็นรูปแบบของ object โดยที่ MVC นั้นกำหนดชื่อ object มาให้เรียบร้อยแล้วตามชื่อเลยก็คือ model view controller ซึ่งการทำงานของทั้ง 3 object 
นี้จะแยกการทำงานอย่างชัดเจนห้ามก้าวก่ายงานกันเด็ดขาด
Model-->การทำงานของ model จะจัดการส่วนที่ข้อมูลทั้งหมดจะคอยเตรียมข้อมูลที่เหมาะสมไว้ และ model นั้นจะทำงานเมื่อ controller ร้องขอเท่านั้น
View-->นั้นจะจัดการส่วนของหน้าตาทั้งหมด หรือส่วนติดต่อกับผู้ใช้โดยตรง (user interface) โดย view นั้นจะรับคำสั่งการทำงานจาก controller และเป็นตัวกลางให้ผู้ใช้ติดต่อกับ controller อีกด้วย
Controller-->เปรียบเสมือนกับมันสมองและศูนย์กลางการทำงานทั้งหมด จะเห็นว่าทุกส่วนนั้นจะติดต่อกับ controller ทั้งหมดรอคอยคำสั่งจาก controller นอกจากนี้ controller จะจัดการทำงานในส่วนที่เป็น logic ทั้งหมดในระบบ
8.ระบบที่มีหน้าที่ในการจัดเก็บการเปลี่ยนแปลงของไฟล์ในโปรเจ็คเรา มีการ backup code ให้เรา สามารถที่จะเรียกดูหรือย้อนกลับไปดูเวอร์ชั่นต่างๆของโปรเจ็คที่ไหน เมื่อไหร่ก็ได้
หรือดูว่าไฟล์นั้นใครเพิ่มหรือแก้ไข หรือว่าจะดูว่าไฟล์นั้นถูกเขียนโดยใครบ้าง
9.ไม่เคย
10.react native เพราะเป็น framework แรกๆที่ได้ใช้งาน เข้าใจได้ไม่ยาก / node.js เคยใช้มาเล็กน้อย
